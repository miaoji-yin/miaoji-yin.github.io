# Images

Replace these files with your artwork images when ready.

- Homepage image path is configured in `src/data/siteContent.js`.
- Project image paths are configured in `src/data/siteContent.js`.
- Images keep their original proportion in the site.
- The longest displayed side is limited to `800px`.
